containers:
  main:
    controller: main
    image:
      repository: ghcr.io/mendhak/http-https-echo
      tag: 29
      pullPolicy: IfNotPresent

ingress:
  main:
    enabled: true

probes:
  liveness:
    enabled: true
  readiness:
    enabled: true
  startup:
    enabled: true
