controllers:
  main:
    type: cronjob
    cronjob:
      timeZone: Etc/UTC
    containers:
      main:
        image:
          repository: ghcr.io/mendhak/http-https-echo
          tag: 30
          pullPolicy: IfNotPresent

service:
  main:
    enabled: false

configMaps:
  config:
    enabled: true
    data:
      test: value 1
